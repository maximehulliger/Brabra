package brabra.gui.controller;

import javafx.event.ActionEvent;
import javafx.event.EventHandler;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import brabra.Brabra;
import brabra.gui.view.ObjectView;

public class ObjectViewController {
	
	/*public Label label;*/
	
	public ObjectViewController(ObjectView view) {
		
		/*Image imageDecline = new Image(getClass().getResourceAsStream(sceneModel.app.dataPathTo("gui/ball-6x6.png")));
		Button button5 = new Button();
		button5.setGraphic(new ImageView(imageDecline));*/
		
		/*button5.setOnAction(new EventHandler<ActionEvent>() {
		    @Override public void handle(ActionEvent e) {
		        label.setText("Accepted");
		    }
		});*/
		
		/*view.btn.setOnAction(
				e -> {
					String currentText = appModel.textToPrint(),
							nextText = currentText + " :D";
					appModel.app.debug.msg(0, appModel.textToPrint(), "tool window");
					appModel.setTextToPrint(nextText+" "+appModel.countObservers());
				});*/
	}

}
